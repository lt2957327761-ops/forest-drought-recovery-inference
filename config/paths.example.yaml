# Replace placeholders with local paths; this file contains no credentials.
project_root: PROJECT_ROOT
zenodo_data_root: ZENODO_DATA_ROOT
release_figure_input_root: RELEASE_DATA_ROOT
output_root: OUTPUT_ROOT
data16_root: DATA16_ROOT
data16_download_dir: DATA16_DOWNLOAD_DIR
source_pack_root: SOURCE_PACK_ROOT

environment_variables:
  NEE_PROJECT_ROOT: PROJECT_ROOT
  NEE_ZENODO_DATA_ROOT: ZENODO_DATA_ROOT
  NEE_RELEASE_DATA_ROOT: RELEASE_DATA_ROOT
  NEE_OUTPUT_ROOT: OUTPUT_ROOT
  NEE_DATA16_ROOT: DATA16_ROOT
  NEE_DATA16_DOWNLOAD_DIR: DATA16_DOWNLOAD_DIR
  NEE_SOURCE_PACK_ROOT: SOURCE_PACK_ROOT
  NEE_SOURCE_DATA_CONFIG: SOURCE_DATA_CONFIG_JSON
