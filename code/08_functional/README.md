# Screens, functional evidence and fire

This stage is deliberately a navigation layer rather than a duplicate code copy.

- Priority A/B/C screen construction and fire-overlap fields: `../03_event_detection/task0010c_events.py`.
- Enrichment, 5° block bootstrap, D3 training-event GPP/NPP contrasts and group evidence/status: `../07_estimands/task0010c_consensus.py`.
- P2 locked-screen estimands: `../04_recovery/task0010e_persistence.py`.

Keeping a single implementation prevents divergence between copied scripts.
