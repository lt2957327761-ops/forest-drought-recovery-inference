name: forest-drought-recovery-inference
channels:
  - conda-forge
dependencies:
  # Exact verified core numerical environment
  - python=3.11.9
  - numpy=2.4.6
  - scipy=1.17.1
  - scikit-learn=1.9.0
  - rasterio=1.4.4
  # Exact verified Python figure/runtime environment
  - matplotlib=3.10.9
  - pandas=3.0.3
  - pillow=12.2.0
  - pip
# Optional IFL packages are intentionally excluded here because their exact
# historical versions were not retained. See requirements-optional-geospatial.txt.
